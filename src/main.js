import $ from 'jquery';
import 'bootstrap';
import 'bootstrap/dist/css/bootstrap.min.css';
import './css/styles.css';

// main.js is for UI logic

// allow users to add tasks to the list (append list items)
// allow users to indicate that a task is done
// allow users to remove a list item (how??)
// write specs in ReadMe.md and run Jest tests?